# コマンド登録スクリプト（npm run register）専用。ローカルでのみ使用する。
# コピーして .env を作成し、値を埋める（.env はコミットしない）。
# 値の取得元: Discord Developer Portal https://discord.com/developers/applications

# General Information > Application ID
DISCORD_APP_ID=

# Bot > Token（Reset Token を押すと表示される）
DISCORD_BOT_TOKEN=
